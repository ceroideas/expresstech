import { InformationPipe } from './information.pipe';

describe('InformationPipe', () => {
  it('create an instance', () => {
    const pipe = new InformationPipe();
    expect(pipe).toBeTruthy();
  });
});
